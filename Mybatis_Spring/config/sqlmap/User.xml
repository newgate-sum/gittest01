<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace:命名空间。目的就是用于：隔离sql语句的作用。类似于java中的包名 -->
<mapper namespace="test">
	<resultMap id="myResultMap" type="com.itheima.mybatis.pojo.User">
		<result property="paraMap.id" column="oId"/>
		<result property="paraMap.number" column="number"/>
	</resultMap>
	<select id="getAllUser" resultMap="myResultMap">
		SELECT u.*, o.id as oId, o.number from `user` u LEFT JOIN `orders` o ON u.id = o.user_id;
	</select>
    <select id="selectUserById" parameterType="int" resultType="User">
		select * from user where id=#{id}
	</select>

    <select id="selectUserByName" parameterType="java.lang.String" resultType="User">
		select * from user where username like '%${value}%'
	</select>

    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
	  insert into user(username,birthday,sex,address)
	  values(#{username},#{birthday},#{sex},#{address})
	</insert>




</mapper>
